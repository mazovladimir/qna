- if (@answer.present? && @answer.errors.any?)
  #error_explanation
    h2
      = pluralize(@answer.errors.count, "error")
    ul
      - @answer.errors.full_messages.each do |msg|
        li #{msg}
p  Вопрос: 
|  #{@question.title}

p  Тело вопроса:
|  #{@question.body}

p Все комментарии:
- @question.answers.each do |comment|
   p #{comment.body} 
   = link_to 'Delete comment', question_answer_path(@question, comment), method: :delete if user_signed_in? && (comment.user_id == current_user.id)

= form_for ([@question, Answer.new ]) do |f|
  p Комментарий:
  |  = #{f.label :body}
  |  = #{f.text_area :body}


  = f.submit 'Comment'
